import Link from "next/link";
import { ArrowUpRight, Clock3, Edit3, FileText, Plus, Radio } from "lucide-react";
import { DeletePostButton } from "@/components/admin/delete-post-button";
import { getAdminPosts } from "@/lib/blog";

export default async function AdminPage() {
  const posts = await getAdminPosts();
  const published = posts.filter((post) => post.status === "published").length;
  return <><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mono text-[9px] tracking-[.2em] text-peak-blue">CONTENT OVERVIEW</p><h1 className="mt-2 text-3xl font-bold sm:text-4xl">Blog Posts</h1><p className="mt-3 text-sm text-black/48">Website journal အတွက် ရေးသား၊ ပြင်ဆင်၊ publish လုပ်နိုင်ပါတယ်။</p></div><Link href="/admin/posts/new" className="flex min-h-12 items-center justify-center gap-2 rounded-xl bg-ink px-5 text-sm font-bold text-white"><Plus size={17} />Post အသစ်ရေးမယ်</Link></div><section className="mt-7 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl border border-black/8 bg-white p-5"><FileText className="text-peak-blue" size={20} /><p className="mt-6 text-3xl font-bold">{posts.length}</p><span className="text-xs text-black/42">စုစုပေါင်း</span></div><div className="rounded-2xl border border-black/8 bg-white p-5"><Radio className="text-peak-blue" size={20} /><p className="mt-6 text-3xl font-bold">{published}</p><span className="text-xs text-black/42">Published</span></div><div className="rounded-2xl border border-black/8 bg-white p-5"><Clock3 className="text-peak-blue" size={20} /><p className="mt-6 text-3xl font-bold">{posts.length - published}</p><span className="text-xs text-black/42">Drafts</span></div></section><section className="mt-6 overflow-hidden rounded-2xl border border-black/8 bg-white"><header className="flex items-center justify-between border-b border-black/8 px-5 py-4"><h2 className="text-sm font-bold">Post အားလုံး</h2><span className="mono text-[9px] text-black/35">LAST UPDATED</span></header>{posts.length ? <div className="divide-y divide-black/8">{posts.map((post) => <article key={post.id} className="grid gap-4 p-5 sm:grid-cols-[1fr_auto] sm:items-center"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><span className={`rounded-full px-2.5 py-1 text-[9px] font-bold uppercase ${post.status === "published" ? "bg-ice text-[#087da0]" : "bg-black/5 text-black/40"}`}>{post.status}</span>{post.featured ? <span className="rounded-full bg-ink px-2.5 py-1 text-[9px] font-bold text-white">FEATURED</span> : null}<span className="mono text-[9px] text-black/30">{post.language.toUpperCase()}</span></div><h3 className="mt-3 truncate text-lg font-bold">{post.title}</h3><p className="mt-1 truncate text-xs text-black/38">/journal/{post.slug} · {new Intl.DateTimeFormat("en-GB", { dateStyle: "medium", timeZone: "Asia/Yangon" }).format(new Date(post.updated_at))}</p></div><div className="flex items-center gap-2"><Link href={`/journal/${post.slug}`} target="_blank" className="grid h-10 w-10 place-items-center rounded-lg border border-black/10" aria-label="View post"><ArrowUpRight size={16} /></Link><Link href={`/admin/posts/${post.id}`} className="grid h-10 w-10 place-items-center rounded-lg border border-black/10" aria-label="Edit post"><Edit3 size={16} /></Link><DeletePostButton id={post.id} /></div></article>)}</div> : <div className="p-12 text-center"><FileText className="mx-auto text-black/15" size={42} /><h3 className="mt-4 text-lg font-bold">Post မရှိသေးပါ</h3><p className="mt-2 text-sm text-black/40">ပထမဆုံး Project Peak Journal post ကို စရေးနိုင်ပါတယ်။</p></div>}</section></>;
}
